/** Evita prerender estático: bundle comercial, validación ERP vs Bsale y fetch al API. */
export const dynamic = "force-dynamic"

export default function ComercialVendedoresLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
