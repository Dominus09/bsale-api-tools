/** Módulo independiente — no prerender (fetch API + sync). */
export const dynamic = "force-dynamic"

export default function NotasCreditoLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
